import { BorderImageDirective } from './border-image.directive';

describe('BorderImageDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderImageDirective();
    expect(directive).toBeTruthy();
  });
});
