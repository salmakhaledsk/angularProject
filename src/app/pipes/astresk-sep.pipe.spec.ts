import { AstreskSepPipe } from './astresk-sep.pipe';

describe('AstreskSepPipe', () => {
  it('create an instance', () => {
    const pipe = new AstreskSepPipe();
    expect(pipe).toBeTruthy();
  });
});
